apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-query-exporter.fullname" . }}
  labels:
    {{- include "helm-query-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-query-exporter.selectorLabels" . | nindent 4 }}